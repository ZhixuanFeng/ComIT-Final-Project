
// -- user code here --
let suitNames = ['diamond', 'club', 'heart', 'spade'];
let effectNames = ['attack', 'block', 'heal', 'mana', 'aoe', 'draw', 'revive', 'hate'];
let cardXPositions = [50, 100, 150, 200, 250];
let cardYPosition = 170;

function cardClicked() {
    this.isSelected = !this.isSelected;
    if (this.isSelected) {
        // addSelectedCard(this);
        // this.border.visible = true;
    }
    else {
        // removeSelectedCard(this);
        // this.border.visible = false;
    }
}

function cancelCardSelection() {
    // removeSelectedCard(this);
    this.isSelected = false;
    // this.border.visible = false;
}

function clearAllCardSelection() {
    hand.forEach(function (card) {
        if(card) card.cancelSelection();
    });
}

function convertNumRankToText(num) {
    switch (num) {
        case 1:
            return 'A';
        case 11:
            return 'J';
        case 12:
            return 'Q';
        case 13:
            return 'K';
        default:
            return num;
    }
}

// function repositionBorder() {
//     this.border.destroy(true);
//     this.border = game.add.graphics();
//     this.border.lineStyle(4, 0xFFFF00, 1);
//     this.border.drawRoundedRect(cardXPositions[this.posIndex] * this.scale.x - 3, cardYPosition * this.scale.y - 3, this.borderWidth, this.borderHeight, 10);
//     this.border.visible = false;
// }

/* --- start generated code --- */

// Generated by  1.5.1 (Phaser v2.6.2)


/**
 * Card.
 * @param {Phaser.Game} aGame A reference to the currently running game.
 * @param {Phaser.Group} aParent The parent Group (or other {@link DisplayObject}) that this group will be added to.
    If undefined/unspecified the Group will be added to the {@link Phaser.Game#world Game World}; if null the Group will not be added to any parent.
 * @param {string} aName A name for this group. Not used internally but useful for debugging.
 * @param {boolean} aAddToStage If true this group will be added directly to the Game.Stage instead of Game.World.
 * @param {boolean} aEnableBody If true all Sprites created with {@link #create} or {@link #createMulitple} will have a physics body created on them. Change the body type with {@link #physicsBodyType}.
 * @param {number} aPhysicsBodyType The physics body type to use when physics bodies are automatically added. See {@link #physicsBodyType} for values.
 */
function Card(aGame, aParent, aName, aAddToStage, aEnableBody, aPhysicsBodyType, positionIndex, cardInfo) {
	
	Phaser.Group.call(this, aGame, aParent, aName, aAddToStage, aEnableBody, aPhysicsBodyType);

    this.posIndex = positionIndex;
    let x = cardXPositions[positionIndex];
    let y = cardYPosition;
    if (cardInfo.taunt > 0) cardInfo.hate = cardInfo.taunt;
    if (typeof(cardInfo.suit) === 'undefined') cardInfo.suit = Math.floor(cardInfo.indecks / 13);
    if (typeof(cardInfo.rank) === 'undefined') cardInfo.rank = cardInfo.indecks - cardInfo.suit * 13 + 1;
    this.info = cardInfo;
    this.cardFrame = this.game.add.button(0, 0, 'card', cardClicked, this, null, 'empty_card', null, null, this);
    if (cardInfo.indecks == 14)
        console.log('yes');
    let numOfEffect = 0;
    for (let i = 0; i < 8 && numOfEffect < 4; i++) {
        let effectName = effectNames[i];
        if (cardInfo[effectName] > 0) {
            let effectIcon = this.game.add.sprite(8.0, 24.0 + numOfEffect * 9, 'card', effectName, this);
            effectIcon.scale.setTo(0.5, 0.5);
            let effectAmount = this.game.add.text(20.0, 22.0 + numOfEffect * 9, cardInfo[effectName], {'font':'bold 20px Arial'}, this);
            effectAmount.scale.setTo(0.5, 0.5);
            numOfEffect++;
        }
    }

    let suit = this.game.add.sprite(3.0, 13.0, 'card', suitNames[cardInfo.suit], this);
    suit.scale.setTo(0.75, 0.75);

    let rankFontStyle = cardInfo.suit === 0 || cardInfo.suit === 2 ? {"font":"bold 20px Arial", "fill":"#ff0000"} : {"font":"bold 20px Arial"};
    let rank = this.game.add.text(4.0, 3.0, convertNumRankToText(cardInfo.rank), rankFontStyle, this);
    rank.scale.setTo(0.5, 0.5);

    this.scale.setTo(2, 2);
    this.position.setTo(x*this.scale.x, y*this.scale.y);

    // this.borderWidth = this.cardFrame.width * this.scale.x + 6;
    // this.borderHeight = this.cardFrame.height * this.scale.y + 6;
    //
    // this.border = game.add.graphics();
    // this.border.lineStyle(4, 0xFFFF00, 1);
    // this.border.drawRoundedRect(x * this.scale.x - 3, y * this.scale.y - 3, this.borderWidth, this.borderHeight, 10);
    // this.border.visible = false;
}

/** @type Phaser.Group */
let Card_proto = Object.create(Phaser.Group.prototype);
Card.prototype = Card_proto;
Card.prototype.constructor = Card;

/* --- end generated code --- */
// -- user code here --
Card.prototype.cancelSelection = cancelCardSelection;
// Card.prototype.repositionBorder = repositionBorder;
